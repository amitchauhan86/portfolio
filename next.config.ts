/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["aceternity.com", "images.unsplash.com", "assets.aceternity.com","upload.wikimedia.org", "peerlist.io"], // Add your domain here
  },
};

module.exports = nextConfig; 